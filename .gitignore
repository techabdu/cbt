# Root-level ignores (each workspace has its own .gitignore too)

# OS / editor
.DS_Store
Thumbs.db
*.swp
/.idea
/.vscode
/.zed
/.nova

# Logs
*.log

# Docker local secrets (never commit)
.env.docker.local
